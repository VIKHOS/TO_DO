spring:
  datasource:
    primary:
      jdbc_url: jdbc:h2:mem:~/todo;DB_CLOSE_DELAY=-1
      username: sa

  flyway:
    url: jdbc:h2:mem:~/todo;DB_CLOSE_DELAY=-1
    user: sa
    baseline-on-migrate: false
    clean-on-validation-error: true
